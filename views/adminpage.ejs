<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%+ title %>
    </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <style>
        .items {
            width: 300px;
            height: 300px;
            border: 1px solid #000;
            padding: 10px;
            border-radius: 8px;
        }

        .items p {
            font-weight: 600;
            line-height: 20px;
            font-size: 16px;
        }

        .items p span {
            font-weight: 400;
            font-style: 14px;
            line-height: 18px;
        }

        .btn a {
            color: #ffff;
            text-decoration: none;
        }

        .btn a:hover {
            color: #ffff;
        }
    </style>
</head>

<body>
    <div class="container">
        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <a class="navbar-brand" href="#">navbar</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item active">
                        <a class="nav-link" href="/admin">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/post">Post</a>
                    </li>
                </ul>
            </div>
            <div>
                <button class="btn btn-warning"><a href="/">log out</a></button>
            </div>
        </nav>
        <main>
            <div class="center w-4">
                <div
                    class="main_content d-flex align-items-center justify-content-center my-3 felx-column gap-3 flex-wrap">
                    <% earthquakeData.forEach(data=> { %>
                        <div class="items">
                            <p class="location">Location: <span>
                                    <%= data.location %>
                                </span></p>
                            <div class="information d-flex gap-3 align-items-center">
                                <p class="longitude">Longitude: <span>
                                        <%= data.longitude %>
                                    </span></p>
                                <p class="latitude">Latitude: <span>
                                        <%= data.latitude %>
                                    </span></p>
                            </div>
                            <div class="next d-flex felx-wrap align-items-center gap-3">
                                <p class="magnitude">Magnitude : <span>
                                        <%= data.magnitude %>
                                    </span></p>
                                <p class="depth">Depth: <span>
                                        <%= data.depth %>
                                    </span></p>
                            </div>
                            <p class="date_time">Date time: <span>
                                    <%= data.date_time %>
                                </span></p>
                            <p class="description">Description: <span>
                                    <%= data.description %>
                                </span></p>
                            <div class="my-4">
                                <button class="btn btn-secondary"><a href="/update/<%= data.id %>">Update</a></button>
                                <button class="btn btn-danger" onclick="deleteItem('<%= data.id %>')">Delete</button>
                            </div>
                        </div>
                        <% }); %>
                </div>
            </div>
        </main>

    </div>

    <script>
        function deleteItem(itemId) {
            fetch(`/delete/${itemId}`, {
                method: 'DELETE',
            })
            .then(response => {
                if (response.ok) {
                    console.log("Data deleted successfully");
                    // Optionally, you can update the UI or take other actions here
                } else {
                    console.error("Error deleting data");
                }
            })
            .catch(error => {
                console.error("Error deleting data:", error);
            });
        }
    </script>
</body>

</html>